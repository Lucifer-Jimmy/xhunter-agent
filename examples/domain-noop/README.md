# No-op Domain

This manifest-only example is used to verify that adding a Domain Pack does not
require a Runtime change. A real Domain supplies its API from a repository-owned
package and composes existing Contracts and Services.
